"use client";
import { useEffect, useState } from "react";
import { useParams } from "next/navigation";
import AppShell from "../../../components/AppShell";
import { api, apiData } from "../../../lib/api";
import type { Email } from "../../../types/email";

export default function EmailDetailPage() { const params = useParams<{ id: string }>(); const [email, setEmail] = useState<Email | null>(null); useEffect(() => { if (params.id) void apiData<Email>(api.get(`/emails/${params.id}`)).then(setEmail); }, [params.id]); return <AppShell><div className="eyebrow">Message detail</div>{!email ? <p className="muted">Loading email...</p> : <><h1>{email.subject}</h1><div className="panel section"><p><strong>From:</strong> {email.sender} &lt;{email.senderEmail}&gt;</p><p><strong>To:</strong> {email.receiver ?? "Unknown"}</p><p><strong>Date:</strong> {new Date(email.receivedAt).toLocaleString()}</p><span className={`badge badge-${email.category}`}>{email.category}</span><hr style={{ border: 0, borderTop: "1px solid var(--line)", margin: "24px 0" }} /><p className="email-body">{email.body}</p></div>{email.analysis && <section className="panel section"><h2>AI security analysis</h2><div className="grid"><div><span className="muted">Spam detection</span><strong>{email.analysis.spamResult} {email.analysis.spamConfidence}%</strong></div><div><span className="muted">Scam detection</span><strong>{email.analysis.scamResult} {email.analysis.scamConfidence}%</strong></div><div><span className="muted">Text risk</span><strong>{email.analysis.textScore}%</strong></div><div><span className="muted">URL risk</span><strong>{email.analysis.urlScore}%</strong></div></div><p className="muted">Keywords: {email.analysis.matchedKeywords.map((item) => item.keyword).join(", ") || "None"}</p><p className="muted">URLs: {email.analysis.detectedUrls.map((item) => `${item.url} (${item.riskScore}%)`).join(", ") || "None"}</p></section>}</>}</AppShell>; }